# PyTorch/torchaudio - Latest versions with CUDA support
# PyTorch 2.8+ required by WhisperX 3.7.4
# Note: PyTorch 2.8 automatically installs nvidia-cudnn-cu12 (cuDNN 9.x) as a pip package
torch>=2.8.0
torchaudio>=2.8.0

# Core dependencies for WhisperX with diarization
# Latest version with improved speaker detection and clustering parameter support
whisperx==3.7.4
transformers>=4.30.0

# Diarization (pyannote) - version will be resolved by whisperx 3.7.4 dependencies
# WhisperX 3.7.4 should support newer pyannote versions with better clustering
pyannote.audio
onnxruntime>=1.15.0

# Audio/video processing
ffmpeg-python>=0.2.0

# Utilities
numpy
tqdm>=4.65.0
psutil>=5.9.0  # For memory monitoring in ModelManager
tomli>=2.0.0;python_version<"3.11"  # TOML config parser (stdlib tomllib in 3.11+)

# Vocal separation (CUDA-accelerated)
audio-separator>=0.18.0
onnxruntime-gpu>=1.19.0  # CUDA acceleration for audio-separator
